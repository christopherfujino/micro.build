target main(test, analyze) {}

target test(packages) {
  run("dart test --reporter=expanded --concurrency=1");
}

target packages() {
  run("dart pub get");
}

target analyze(packages) {
  run("dart analyze . --fatal-warnings --fatal-infos");
}
